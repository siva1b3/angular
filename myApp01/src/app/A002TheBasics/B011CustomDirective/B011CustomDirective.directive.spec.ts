/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { B011CustomDirectiveDirective } from './B011CustomDirective.directive';

describe('Directive: B011CustomDirective', () => {
  it('should create an instance', () => {
    const directive = new B011CustomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
